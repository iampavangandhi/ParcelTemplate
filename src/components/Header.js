const Header = () => {
  const template = `
    <header>
      <h1>Parcel App</h1>
      <p>Basic workflow starter for building vanilla JS apps using the Parcel bundler.</p>
    </header>
  `;

  return template;
};

export default Header;
